language: python
    python:
      - "3.7"
    services:
      - sqllite
    env:
      -DJANGO=2.1.2 DB=sqllite
    install:
      - pip install -r requirements.txt
    before_script:
      - sqllite -e 'create database test;' -u root
    script:
      - python manage.py test
